server {
    listen 80;
    server_name {{inventory_hostname}};
    root /var/www/html;
    index index.html index.htm;
    access_log /var/log/{{ansible_user_id}}.nginx.log;
    error_log /var/log/{{ansible_user_id}}.nginx.err;
    location / {
        try_files $uri $uri/ =404;
    }
}